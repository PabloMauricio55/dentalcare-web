import { BillingShell } from "@/modules/billing/components/BillingShell";
export default function Layout({ children }: { children: React.ReactNode }) { return <BillingShell>{children}</BillingShell>; }
