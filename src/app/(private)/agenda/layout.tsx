import { AgendaShell } from "@/modules/appointments/components/AgendaShell";
export default function Layout({ children }: { children: React.ReactNode }) { return <AgendaShell>{children}</AgendaShell>; }
